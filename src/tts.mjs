import { createTTS } from './tts-controller.mjs';
import { chunkText, deduplicateAndSortVoices, resolveVoiceIndex, tokenizeReaderDOM } from './tts-core.mjs';

export { chunkText, deduplicateAndSortVoices, resolveVoiceIndex, tokenizeReaderDOM };
export { createTTS };
